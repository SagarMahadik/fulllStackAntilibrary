/**
 * File name : genreDetails.js
 * Possible modifications:
 * There should be a single svg component
 * Maintain genre name in the array
 */

import styled from 'styled-components';
import { stopColor1 } from '../../../styles/applicationStyles/genreTheme';

export const GenreStop = styled.stop`
  stop-color: ${stopColor1};
`;

export const GenreStop1 = styled.stop`
  stop-color: ${stopColor1};
`;
